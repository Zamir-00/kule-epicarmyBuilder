{
  "name": "@kule/api",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Backend API for Kule Army Builder",
  "scripts": {
    "dev": "tsx watch src/index.ts",
    "build": "tsc",
    "start": "node dist/index.js",
    "test": "node --test --import tsx src/__tests__/**/*.test.ts",
    "typecheck": "tsc --noEmit",
    "migrate": "echo 'No migrations yet (S2.3)'"
  },
  "dependencies": {
    "@fastify/static": "^8.3.0",
    "fastify": "^5.0.0",
    "zod": "^3.23.0"
  },
  "devDependencies": {
    "@types/node": "^20.14.0",
    "tsx": "^4.16.0",
    "typescript": "^5.4.0"
  }
}
